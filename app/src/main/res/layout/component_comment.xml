<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/commentBgView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/spacing_x16"
        android:layout_marginLeft="@dimen/spacing_x16"
        app:layout_constraintStart_toEndOf="@+id/userIconBgView"
        app:layout_constraintTop_toTopOf="parent"
        android:padding="@dimen/spacing_x16">

        <TextView
            android:id="@+id/authorNameTextView"
            style="@style/Body3"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/mineShaft"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/bodyTextView"
            style="@style/Body1"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_x8"
            android:textColor="@color/tundora"
            app:layout_constraintTop_toBottomOf="@+id/authorNameTextView" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</merge>